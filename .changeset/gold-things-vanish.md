---
'safeform-example-next': minor
'@safeform/core': minor
---

Added `maskToZod(mask, message?)` to generate a Zod schema from any mask name or custom pattern, and `MASK_SCHEMAS` — a pre-built map of Zod
